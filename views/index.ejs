<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Capital Quiz</title>
    <link rel="icon" type="image/png" href="/images/favicon.png">
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body id="app">
    <form action="/submit" class="container" method="POST">
        <div class="horizontal-container">
            <h3>
                Total Score:
                <span id="score">
                    <%= typeof totalscore !== "undefined" ? totalscore: 0 %>
                </span>
            </h3>
        </div>

        <% if (question && question.country==="Game Over") {%>
            <h1 id="countryname">Game Over</h1>
            <a href="/"class="restart-button">Restart</a>
        <%} else {%>
        <h1 id="countryname">
            <%= question.country %>
        </h1>
        <div class="answer-container">
            <input type="text" name="answer" id="userInput" placeholder="Enter the Capital" autofocus autocomplete="off" required>
        </div>

        <button type="submit">SUBMIT <%if(locals.wasCorrect === true){ %>
            <span class="checkmark">✔</span>
       <% } %>
        </button>
        <% } %>
    </form>
</body>
</html>